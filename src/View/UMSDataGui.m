% A way to display ums data

classdef UMSDataGui < handle
    properties (Access = private)
        
    end

    methods % public
        function obj = methodName(obj,args)

        end
    end % public methods

    methods (Access = private)
        function obj = initGui(obj)
            
        end
    end % private methods
end